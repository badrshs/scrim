[package]
name = "scrim"
description = "Watch movies with explicit content covered. Everything runs locally."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[lib]
name = "scrim_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
scrim-core = { path = "../crates/scrim-core" }
scrim-detect = { path = "../crates/scrim-detect" }
scrim-mpv = { path = "../crates/scrim-mpv" }
scrim-cast = { path = "../crates/scrim-cast" }
tauri = { version = "2", features = ["devtools"] }
tauri-plugin-dialog = "2"
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { version = "1", features = ["rt", "sync", "time", "macros"] }

[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = [
    "Win32_Foundation",
    "Win32_UI_WindowsAndMessaging",
    "Win32_Graphics_Gdi",
    "Win32_System_LibraryLoader",
] }
