{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "Scrim",
  "version": "0.1.0",
  "identifier": "app.scrim.player",
  "build": {
    "frontendDist": "../ui"
  },
  "app": {
    "withGlobalTauri": true,
    "windows": [
      {
        "title": "Scrim",
        "width": 1440,
        "height": 900,
        "minWidth": 900,
        "minHeight": 560,
        "decorations": false,
        "transparent": true,
        "dragDropEnabled": true,
        "theme": null
      }
    ],
    "security": {
      "csp": "default-src 'self'; img-src 'self' data: asset: http://asset.localhost; style-src 'self' 'unsafe-inline'; font-src 'self' data:; media-src 'self' asset: http://asset.localhost; connect-src 'self' ipc: http://ipc.localhost"
    }
  },
  "bundle": {
    "active": true,
    "targets": ["nsis"],
    "resources": ["../resources/*"],
    "license": "../LICENSE",
    "shortDescription": "Watch movies with explicit content covered.",
    "longDescription": "Scrim plays movies with explicit nudity covered by a box or a blur, composited live so the original file is never re-encoded. Detection, playback, and casting all run on your own machine.",
    "windows": {
      "nsis": {
        "installMode": "perMachine"
      }
    }
  }
}
